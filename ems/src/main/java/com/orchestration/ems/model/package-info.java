/**
 * Immutable domain records: {@code EventRow}, {@code ContextRow}, {@code EnrichedEvent}
 * (the forwarded conf shape — identical to today in Phase A; {@code contractVersion}
 * deferred to Phase B per Amendment A5), {@code SubscriptionRow}, and {@code SubscriptionMatch}.
 * See ems-design.md §5.
 */
package com.orchestration.ems.model;
